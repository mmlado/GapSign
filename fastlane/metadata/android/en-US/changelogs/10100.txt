Codecov coverage upload in CI and a live README coverage badge Decode ERC-20 calldata (`transfer`, `transferFrom`, `approve`) in transaction review; show unlimited approval warning Detect and label pre-hashed EIP-712 payloads (`0x1901`) with domain separator and message hash `full` (`tech.gapsign`) and `offline` (`tech.gapsign.offline`) Android build flavors QR code fallback for external links so air-gapped users can scan URLs on another device EIP-712 JSON decode for minified or non-round-tripping UTF-8 payloads; prettify JSON display
